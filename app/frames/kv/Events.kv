#: import h app.frames.utilities.scrH

<Events>:
    orientation: 'vertical'

    AnchorLayout:
        position_x: 'left'
        position_y: 'center'
        size_hint: (None, None)
        padding: [self.parent.width / 3, 0, 0, 0]

        MonoLabel:
            text: 'Upcoming events:'
            size_hint: (None, None)
            size: self.texture_size

    BoxLayout:
        orientation: 'vertical'


        Event:

        Event:

        Event:

        Event:

        Event:

        Event:




<Event>:
    StackLayout:
        minimum_width: self.parent.width - self.parent.width / 5
        x: self.parent.x + self.parent.width / 10
        orientation: 'lr-tb'
        MonoLabel:
            #name: eventtime
            size_hint: (None, None)
            size: self.texture_size

            text: "XX:XX"

        MonoLabel:
            #name: spacer
            size_hint: (None, None)
            size: self.texture_size

            text: "  "

        MonoLabel:
            #name: event
            size_hint: (None, None)
            size: self.texture_size

            text: "Lorem ipsum dolor sit amet"

